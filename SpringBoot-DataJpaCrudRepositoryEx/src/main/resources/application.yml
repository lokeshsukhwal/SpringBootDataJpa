spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver # [optional]
    url: jdbc:mysql://localhost:3306/boot7am
    username: root
    password: root
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect # [optional]
    hibernate:
      ddl-auto: update
      